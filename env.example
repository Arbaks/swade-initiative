# Firebase Web API key. This value is public at runtime, but keeping it here as a placeholder
# avoids committing the concrete project key into the source repository.
VITE_FIREBASE_API_KEY=your_firebase_web_api_key
